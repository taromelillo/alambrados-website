---
const { icon, title } = Astro.props;
---

<div class="flex items-center min-w-[300px] max-w-[350px] gap-3">
  <p class="p-2">{icon}</p>
  <div class="flex flex-col">
    <h3 class="font-semibold text-lg">{title}:</h3>
    <slot />
  </div>
</div>
